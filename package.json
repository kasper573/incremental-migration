{
  "private": true,
  "scripts": {
    "build": "pnpm --filter bootstrap build",
    "dev": "pnpm --filter bootstrap dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "tsconfig": "workspace:*"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "incremental-migration"
}
